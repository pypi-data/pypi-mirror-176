#!python
import os
import sys
from importlib import resources
with resources.path('lalapps', '__init__.py') as new_path:
    new_path = str(new_path.parent / 'bin' / 'lalapps_coh_PTF_inspiral')
os.execv(new_path, sys.argv)