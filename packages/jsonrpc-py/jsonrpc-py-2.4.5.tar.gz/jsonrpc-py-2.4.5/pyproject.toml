[build-system]
# The minimum setuptools version is specific to the PEP 517 backend,
# and may be stricter than the version required in setup.cfg:
requires = ["setuptools>=40.6.0,!=60.9.0", "wheel"]
build-backend = "setuptools.build_meta"
