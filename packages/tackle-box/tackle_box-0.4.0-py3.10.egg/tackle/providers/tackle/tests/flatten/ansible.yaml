

ansible<-:
  input:
    type: str
    description: The input location to run against
    default: ansible-hello-world

  ask-vault-password:
    type: bool
    description: ask for vault password
    default: false

  exec:
    args->: flatten ansible
    cmd->: command ansible-playbook {{args}}

#  playbook:
#    type: str
#    description: Playbook(s)

#  args: ['playbook']

#t->: flatten

c->: ansible --ask-vault-password

