[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "temporalvector"
version = "0.1.0"
authors = [
  { name="Chris McKay", email="crmckay55@gmail.com" },
]
description = "Temporally aware vector of values that can be aggregated using different methods over different time periods"
readme = "README.md"
requires-python = ">=3.10"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

[project.urls]
"Homepage" = "https://git.jetbrains.space/cmckay/advanced-simulation/pkg_temporal_vector.git"
