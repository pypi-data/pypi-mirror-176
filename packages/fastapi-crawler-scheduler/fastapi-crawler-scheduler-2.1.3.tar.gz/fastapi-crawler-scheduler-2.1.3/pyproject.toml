[tool.poetry]
name = "fastapi-crawler-scheduler"
version = "2.1.3"
description = "修复apscheduler，任务调度的bug"
authors = ["laowang <847063657@qq.com>"]
readme = "README.md"
packages = [{ include = "fastapi_crawler_scheduler" }]

[tool.poetry.dependencies]
python = "^3.7"
uhashring = "^2.1"
APScheduler = "^3.9.1"
redis = "^4.3.4"
fastapi = "^0.85.0"
fastapi-utils = "^0.2.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
