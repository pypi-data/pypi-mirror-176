{
  "description": "POST request to merge two documents",
  "type": "object",
  "properties": {
    "source_document_id": { "type": "integer" },
    "target_document_id": { "type": "integer" },
    "comment": { "$ref": "entities/comment.json" }
  },
  "required": ["source_document_id", "target_document_id", "comment"],
  "additionalProperties": false
}
