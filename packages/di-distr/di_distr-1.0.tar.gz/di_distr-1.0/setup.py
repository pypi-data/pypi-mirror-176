from setuptools import setup

setup(name='di_distr',
      version='1.0',
      description='Gaussian and Binomial distributions',
      packages=['di_distr'],
      zip_safe=False,
      py_modules=[],
      author="Diana")
