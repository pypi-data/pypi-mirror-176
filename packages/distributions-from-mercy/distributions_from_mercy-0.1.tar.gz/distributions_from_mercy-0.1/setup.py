from setuptools import setup

setup(name='distributions_from_mercy',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions_from_mercy'],
      zip_safe=False)
