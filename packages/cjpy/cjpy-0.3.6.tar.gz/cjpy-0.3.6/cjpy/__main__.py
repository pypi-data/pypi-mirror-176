
import cjpy.cjson as cj

if __name__ == "__main__":
	""" 
	    to run in command line
		cjpy json_file [json_files ...] [-other parameters]

	    to run in ipython
		cfg=cj.get_parameters(['json file','other parameters'])
		out=cj.execute(cfg)
	"""
	cj.go()
	""" """
